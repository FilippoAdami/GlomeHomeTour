Versioned contracts shared across subsystems: VIO pose JSON, floor-plan format, panorama
manifest. Does NOT include the mobile depth-guidance point cloud format, which is
intentionally kept separate (guidance-only, never sent to the backend as a reconstruction
input) — see `mobile/depth-guidance/README.md`.
