Frame extraction, keyframe downsampling, and timestamp-to-pose matching from uploaded
video + VIO trajectory JSON. Produces hybrid SfM pose priors (VIO poses seeding a refinement
solver) consumed by `../reconstruction/`.
