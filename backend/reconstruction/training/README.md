2DGS optimization loop: density control, spherical-harmonics/appearance pruning, model
quantization for the compressed web-delivery format. Calls into `../rasterizer_hip/` (or
`../rasterizer_torch_fallback/` for debugging).
