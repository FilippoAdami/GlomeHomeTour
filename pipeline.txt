Pipeline Verification & SequencingBecause Depth Anything V3 explicitly uses the camera parameters to ground metric scale:Your step ordering is valid:Acquisition: Capture video $\to$ transforms.json.COLMAP Refinement: Refine/anchor poses and intrinsics in sparse/0/ to correct smartphone optical calibration.Conditioned Depth Inference: Feed the RGB frames together with the refined intrinsics/poses into Depth Anything V3 $\to$ generates geometrically consistent, metric depth maps.Dense Point Cloud Back-Projection: Unproject the metric depth maps into a coherent dense point cloud (eliminating the need for manual affine scale/shift alignment).2DGS Training: Initialize Gaussians on this dense point cloud using Depth-Normal priors, keeping live camera pose updates strictly bounded to prevent optimization drift.Critical Practical CheckEnsure the intrinsics passed to Depth Anything match the exact normalized focal length and principal point format the model expects (e.g., OpenCV pixel convention vs. normalized $[-1, 1]$ screen coordinates).Pass the COLMAP-refined focal length ($f_x \approx 1477$, $f_y \approx 1529$), as feeding the inaccurate raw phone focal length ($1406\text{ px}$) will warp the metric scaling of the generated depth maps.